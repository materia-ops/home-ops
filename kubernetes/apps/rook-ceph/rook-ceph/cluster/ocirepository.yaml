---
# yaml-language-server: $schema=https://schemas.materia.wtf/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: rook-ceph-cluster
spec:
  interval: 15m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    # helmrelease prometheusRuleOverrides + ./prometheusrule.yaml patch this
    # chart's alert rules by name — re-check both on bump
    tag: v1.20.6
  url: oci://ghcr.io/rook/rook-ceph-cluster
